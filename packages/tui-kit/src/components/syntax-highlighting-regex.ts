/** Fixed regular expressions used while decoding highlighted HTML. */

export const HIGHLIGHT_CLASS_ATTRIBUTE_PATTERN = /\sclass\s*=\s*(?:"([^"]*)"|'([^']*)')/u;
export const HIGHLIGHT_CLASS_SEPARATOR_PATTERN = /\s+/u;
