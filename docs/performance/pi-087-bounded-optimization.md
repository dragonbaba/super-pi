# Pi v0.87.0 bounded optimization

This is the single working evidence record for `perf/pi-087-bounded-optimization`.
Status is **ready for review; pending merge**, not a release or billing claim.

- Actual origin/main starting commit: `843e96f098ab5e250bb74794ffc921cc6a7daa04` (also the historical assessment coordinate; no intervening changes).
- Upstream: only `earendil-works/pi` tag `v0.87.0`, independently resolved by `git ls-remote` and fetched Git object to `16787ad5b2dc748047f314ca1bfe7708f30f54f3`.
- Existing untracked `.codegraph/`, `.sp/project-*`, and both supplied optimization plans are user-owned and excluded from task commits.
- Environment: Windows, Node v26.4.0, npm 12.0.1, Intel i7-14700KF (20 cores / 28 logical processors). CI uses Node 22.19 on Linux and Windows. All provider fixtures are synthetic, offline fetches; no paid experiments.
- Baseline `npm.cmd run build:offline`: passed. Measurement samples, fixtures and exact measured commits are recorded below.

| Item | Current evidence / implementation decision | Commit | Tests / benchmark | Conclusion |
| --- | --- | --- | --- | --- |
| S1 overflow | Scope bodyless 400/413 to Cerebras; recognize explicit z.ai phrase | `8fc432026` | 9/9 boundary + actual SDK wire tests; AI offline build and check passed | 已实现并验证 |
| S2 strict | Conservative Chat default; explicit compat/capability support; retain built-in support as explicit metadata | `e01aab5d4` | 68/68 related tests; all built-in Chat catalog entries through serializer; local strict=require zero sends | 已实现并验证 |
| S3 GIF | Require full GIF87a/GIF89a signature | `ecce87888` | 6 red cases before; 9/9 after; image/read related suite 61 passed, 1 platform skip | 已实现并验证 |
| S4 FIFO | Replaced queue and waiting arrays with incoming/outgoing FIFO stacks | `6c47620fc` | 5/5 FIFO/lifecycle tests; queue microbench low 128/high 10k; production `bench:stream` 100k | 已实现并验证；普通场景收益不稳定 |
| S5 virtual payload | Keep jiti lazy; load the virtual module map only for source TypeScript and Bun extension paths, while built Node uses aliases | `2d217e8fb`, `1113edb5c` | source, built Node and Bun binary lifecycle tests; 10-sample startup and first/repeat extension-load measurements | 已实现并验证；构建 Node 进一步消除载荷导入 |
| S6 fuzzy | Advance the existing character search with `indexOf`; retain current scoring and Unicode boundary rules | `636ba2668` | 2 semantic suites; old/new corpus checksum equal; 20-sample CPU and retained-heap comparison | 已实现并验证；仅 CPU 有稳定收益 |
| R1 request history | Length → compaction → retry now projects the abandoned assistant out of retainedTail while preserving its durable entry and usage | `2ee91cb9b` | New real JSONL/provider-wire regression 11/11; focused recovery test; existing recovery/stream suite 128/128 | 已复现并最小修复；已验证 |
| R2 TUI | Existing retained viewport, incremental Markdown, frame queue and lifecycle paths satisfy the contract; no reproduced gap | no source change | focused TUI suite 209/209; transcript/frame/session benchmarks passed; deterministic reduced Markdown benchmark passed | 已有正确实现；无缺口修改 |
| R3 budgets | Existing V2/G2D/PrefixManifestRecorder behavior remains bounded and source/UI complete | no source change | budget/prefix production suites 113 passed, 1 existing skip; prefix and budgeted/contextual benches passed | 已有正确实现；离线验证，无计费声明 |

No compile cache, paid warming, model/auth addition, telemetry, RPC/state/context platform, dependency or product version update, renderer rewrite, merge, deployment, or branch-protection change belongs to this task.

## Verification and review

S3 keeps the existing image processor, size and read-identity paths. Valid one-pixel GIF87a/GIF89a files pass actual `createReadToolDefinition.execute` and CLI `processFileArguments` with default image processing enabled; 3–5 byte prefixes, wrong version, text beginning GIF and lowercase text stay text. Related `image-acceptance-closeout` and `read-windowing` tests pass; one existing platform-specific test is skipped, not counted as a pass.

S4 audited the production path `provider adapter → AssistantMessageEventStream.push → Agent async consumer → event delivery`; no provider delta or TUI latest-only behavior was changed. The same Windows/Node v26.4.0 fixture used warmup 5 and samples 20 on an Intel i7-14700KF, with a 100,000-delta production `bench:stream` and a queue-only 128/10,000 prequeue probe. The old `ecce87888` production run was p50/p95/p99 28.66/29.78/33.97 ms; candidate `6c47620fc` was 24.91/26.99/30.83 ms, with `eventsDelivered=1`, `maxPendingKeys=1`, and `coalesced=100001`. A second old/candidate micro run was noisy: low backlog p50 0.0192/0.0218 ms and high backlog p50 1.1021/1.1217 ms; high-backlog p95 was 1.982/1.410 ms. Structural counters consumed 129/10001 events and left queue/waiter lengths at zero in 25/25 runs. This does not establish a stable ordinary-scene speedup or a billing/token change; the change is retained as the bounded upstream FIFO representation with no measured regression beyond normal variance. No Promise, AbortController, pool, backpressure or frame-path allocation was introduced.

S2 changes only Chat Completions defaults, not Responses defaults. An explicit capability supports strict when compat is absent; either explicit compat false or capability false vetoes it. Existing built-in behavior is retained by adding positive compat metadata to the existing catalog (no refreshed model IDs/prices/limits). The generator's delta baseline is also false, so future generation emits positive support explicitly. The catalog migration was previewed, deterministic and idempotent; existing explicit false/true values were untouched and content hashes updated. Unknown endpoints preserve optional parameters for `prefer`; `require` fails locally with zero sends. The local argument validator remains active. S1's actual session-wire fixture now also declares a prefer-strict optional tool and checks the final wire before asserting bounded recovery counts.

S1 remote checks passed for `8fc432026` only: [CI](https://github.com/dragonbaba/super-pi/actions/runs/35673987821). [Codex request](https://github.com/dragonbaba/super-pi/pull/42#issuecomment-5769732370), [actual S1 result](https://github.com/dragonbaba/super-pi/pull/42#issuecomment-5769753951). Conversation, formal reviews and inline comments were read; no findings on that head. Required checks queried from branch protection are `verify-linux` and `verify-windows` with strict up-to-date requirement. Later heads still need their own checks/review.

S1 baseline regression: 4 failures / 9 tests before the classifier change (generic classification, both non-Cerebras bodyless SDK cases, z.ai SDK case), 9 passes after. Ordinary compatibility, 429, and 503 errors each send one ordinary request and zero summaries. Repeated genuine overflow sends two ordinary requests, one compaction operation and exactly two summary requests (history + split-turn prefix), then stops; raw failed history survives. Silent overflow, cache-read accounting and length/zero-output boundaries remain tested. This proves offline request counts, not paid cost reduction. The path audited is SDK runtime → OpenAI SDK fetch → assistant error → AgentSession `_checkCompaction` → existing bounded `_runAutoCompaction` → serializer. No recovery lifecycle or hot delta allocations changed.

S5 moved the existing virtual module map and its runtime imports to `virtual-modules.ts`. `loader.ts` keeps a cached dynamic import only for source TypeScript and Bun; built Node uses the existing resolved aliases and never imports the virtual payload. The source test checks that payload imports are absent from loader startup, concurrent source loads share one promise, and a failed extension can be retried. The built Node test exercised `dist` aliases; a Bun 1.4.0 compiled smoke binary exercised the `$bunfs` branch. Both checked modern/legacy aliases through a shared exported function, TypeBox loading, concurrent loads and zero load errors. On Windows Node v26.4.0, Intel i7-14700KF, warmup 3 and 10 samples, the old `e9759b669` and final S5 candidate `1113edb5c` used the same `.sp` resources and `--help` fixture: `scripts/superpi.mjs` default resource startup was 1579.976 ms versus 1476.802 ms median (old versus candidate; ranges 1557.177–1598.515 and 1448.550–1524.690 ms); `dist/cli.js --no-extensions --help` was 532.379 versus 538.609 ms (ranges 521.888–547.050 and 526.099–562.767 ms), so the no-extension result is treated as startup noise rather than a stable gain. A same-process built Node extension fixture measured first load at 257.998 ms median (250.339–281.074) and cached repeat at 0.091 ms (0.088–0.102). That first load still includes jiti and extension evaluation; the static virtual payload import was eliminated from built Node startup. The payload cache intentionally follows process lifetime; extension cache invalidation still controls extension factories. No Node compile cache was added. No separate Bun performance benchmark was used; the Bun result is a functional compile/run check.

S6 changes only the progression inside the existing fuzzy matcher: `indexOf` finds the next matching code unit while the current score, tokenization, Unicode whitespace, punctuation boundaries, numeric/letter swap and stable sort behavior remain unchanged. The semantic oracle covers empty queries, case, Chinese, whitespace, punctuation, continuous matches, numeric/letter exchange and filter ties; both suites pass and the old/new benchmark checksum is `-58159400` for both. On Windows Node v26.4.0, Intel i7-14700KF, warmup 5 and 20 samples, the same 20,000-item/5-query corpus performed 250,000 direct matches and 500,000 filter items per sample: old `e9759b669` was 108.710 ms median / 110.232 ms p95, candidate `636ba2668` was 88.712 ms / 91.789 ms. Explicit GC after each sample left 0-byte median retained heap delta for both; the earlier work-heap sample was noisy, so no memory improvement is claimed. No repeated normalization, closure removal, global index or cache was added.

R1 initially appeared correct because the earlier fixture disabled compaction and reused one in-memory SessionManager. The new `tests/pi087-length-recovery.test.ts` reproduced three failures before the fix: after a real `finish_reason=length` response, successful compaction saved that response inside `retainedTail`, so the sentinel reappeared on a fresh JSONL reopen and on a recovered branch/model switch. The minimal fix uses a projected branch only for the retriable overflow compaction preparation, extension hook and accounting; the original message entry, usage and session file remain unchanged. After the fix the test passes 11/11 with 6 ordinary provider requests and 1 summary request: A immediate recovery, B next ordinary turn, C fresh JSONL reopen and D recovered branch/model switch all omit `ABANDONED_LENGTH_SENTINEL`; a branch before the recovery decision still includes it. The same fixture preserves the completed tool-call/result pair, fixed system prompt and tool declaration, executes the pre-recorded side effect zero times, and retains the raw length entry with output/reasoning/total usage and cost. Summary error/cancel preserve the sentinel; a later un-compacted length response remains present; later error/abort remains filtered. `tests/pi087-request-history.test.ts` still passes, and the existing recovery/stream/compaction suite passes 128/128. No ContextEditEntry, TranscriptContext or lifecycle migration was introduced.

R2 found no reproducible TUI gap and changed no TUI source beyond S6 fuzzy matching. The focused lifecycle and source-invariant suite passed 209/209. On the same Windows Node v26.4.0 host, `bench:tui-transcript` used the existing 5,000-item, 120x40, slow-16KiB/s fixture with warmup 5 and 20 measured runs: p50/p95 10.6783/10.7039 ms, visible lines 40, one active item render, zero full-history fallbacks, terminal active-write high water mark 1. `bench:tui-frame-allocations -- --fixture production-main --items 50000` used 1,000 warmup and 20,000 measured frames: CPU p50/p95 0.0217/0.0302 ms, sampled 7,235.95 bytes/frame, full-size frame copies 0, frame promises/abort controllers/wrappers 0, terminal queue/active-write/pending high water marks 1/1/0, and all tracked retained references were 0 after dispose. `bench:tui-session-event-allocations` used 100,000 updates: direct message/tool lanes reported 0 promise/array/wrapper counters per update; the coalesced observer lane delivered one snapshot from 100,000 raw updates. The default full Markdown benchmark exceeded the practical local run window and was stopped by exact process identity; it is not counted as passed. The reduced deterministic run completed with one process, 200 updates, 50 warmup updates, two lifecycle cycles and all existing fixtures/widths; incremental fixtures retained 200 eligible updates with zero full fallbacks, and the lifecycle heap slope was -3,712 bytes/cycle with zero cached source/rendered lines after invalidation.

R3 found no budget or projection gap and changed no budget source. The production test suite passed 113 tests with one existing measurement-child skip. It covers default/V1 behavior, explicit `enabled:true` with positive budgets, too-small budgets, 2/4/8 and 129-result rounds, history capacity, continuation, resume, model/context changes, provider wire projection, UI full-source retention and lifecycle release. `bench:prefix` used 100 orderings and 64 resources with zero manifest hash drift. The budgeted and contextual model-view benches reported zero full-result copies/serializations, zero per-result maps/sets/closures/object pools, bounded token envelopes, zero active coordinators after cleanup and zero retained weak references in the contextual lifecycle. These are offline estimates and provider fixtures; no real billing or paid-model reduction is claimed.

Each implemented slice receives its own commit after the smallest regressions. This follow-up adds one focused production fix and one regression file; it does not expand S1–S6. Local completion gates are `check`, `build:offline`, `test:hot`, full `test`, and the relevant recovery/compaction suites. The current follow-up HEAD still requires its own Linux/Windows Actions and Codex review; pending, skipped, or old-head results do not count as success.
