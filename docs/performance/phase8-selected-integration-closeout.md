# Phase 8 selected integration closeout

Base: f419b4985b487756faa3398e02f380acaa6d689d (normal main CI34254206333 successful).
Preserved parent: 9e0e43ff87951a246dcaeb89e80504da6d6079e9, four documentation/comment files only.
7B is deferred: AgentHarness has no runnable event producer; no wrapper or orchestration is implemented.

## Coverage and evidence applicability

All existing fixtures below are at the base revision. Their runtime callers are unchanged by7A or this validation slice.
CI34254206333 supplies reused-valid normal-test evidence, not fresh timing or proof of a new connection.
Final exact candidate CI must rerun normal discovery; no historical standalone benchmark campaign is repeated.
New connections are identified explicitly; isolated unit results are not promoted to integration evidence.

| v1.3 catalogue | Existing test/evidence | Selected connection / disposition |
| --- | --- | --- |
|1 Long text conversation|tui-retained-session-rebuild; prefix-production-assembly|Retained history evidence reused; bounded repeated runs fresh, no overnight conversation claim|
|2 Reasoning stream|provider-contract fixtures; tool-progress-backpressure|Offline protocol coverage reused; real-provider reasoning excluded|
|3 100,000 deltas|event-delivery; tool-progress-backpressure|Existing100,000-update bounds reused; new workload uses32 updates/tool|
|4 Parallel tools|tool-progress-backpressure four-tool case|Fresh two-tool run/abort/next-run with slow frame sink|
|5 Frequent progress|tool-progress-backpressure|Fresh terminal/observer settlement connection|
|6 MCP deferred activation|mcp-client-boundary covers pending-call ownership, not deferred activation|Deferred activation end-to-end remains an explicit unverified gap; no live service or Harness runtime is claimed|
|7 Dynamic instructions|prefix-production-assembly; prefix-production-diagnostics|Existing drift causes reused; unchanged provider-context prompt/tools check fresh (not transport cache-hit evidence)|
|8 Compaction/resume|evidence-ledger-session; alpha-compaction-quit|Existing boundaries reused; long-run automatic-compaction combination not newly measured|
|9 Model switch|model-capability-context|Existing active-tool restoration/capability conversion reused|
|10 Unknown custom model|model-resolution-compat; provider-contract|Offline existing support/refusal contracts reused|
|11 50,000 transcript|tui-retained-session-rebuild; retained benchmark evidence|No new50,000-history benchmark; no claim about Harness restore|
|12 Slow terminal|tui-frame-queue; alpha-lifecycle|Fresh Agent observer -> real TerminalFrameQueue with gated sink; not a new full TUI test|
|13 Slow observer|event-delivery; tool-progress-backpressure|Fresh awaited observer and end-of-run flush across repeated runs|
|14 Hook error/timeout|extension-hook-timeout; operation-write-session|Existing fail-open/closed and host failure evidence reused|
|15 Large results|tool-result-contextual-budget; budgeted-model-view; read-windowing|Fresh real-dispatch read context checked; continuation regressions reused|
|16 Evidence reuse|evidence-ledger-session/benefit-path/owner|Fresh sequential provider dispatch, hit then target mutation and reread|
|17 Side-effect recovery|operation-crash/write-session/e2|Fresh recovery -> ordinary provider request, factual history and no replacement effect|
|18 Windows PowerShell|powershell|Existing shell tests; protected-write safe refusal separately asserted|
|19 Linux Bash|evidence-ledger-session; tool-result-presentation-production|Existing shell invalidation/presentation only; no new native Bash end-to-end measurement or shell replay support|
|20 Session/fork/tree|alpha-session-replacement; evidence-ledger-session; operation-write-session|Existing isolation reused; fresh request/disposal release|

Provider smoke catalogue (OpenAI/Codex, Anthropic, Google, DeepSeek, OpenRouter, compatible custom and unknown model):
only existing offline provider-contract coverage applies. No credentials, real services or paid smoke.
The8.5 comparison is architectural only: typed event/policy/transport boundaries and budgets remain;
repo-map/LSP envelopes, workflow/Harness migration and multi-client expansion are not implemented.

## Fixed fresh workload and budget

One small fixture composes production Agent dispatch with TerminalFrameQueue using an offline stream and gated sink.
Two warmup cycles, then eight measured cycles; each cycle has two tools,32 progress updates/tool,
a supported abort after an update, awaited final delivery, and a subsequent successful run.
Check at every idle boundary: pending events/tools/frames zero; pending key cap2, frame queue cap2;
final update precedes tool end, agent_end delivery drained, no listeners left after unsubscribe.
Canonical history is intentionally retained until the explicit Agent reset boundary, not counted as a leak.
No complete transcript/payload allocation is used as a key and no production instrumentation is added.

Two focused SDK connections use existing evidence and operation fixtures:
sequential read/reuse/file-change through the real dispatcher, and native protected historical recovery
followed by a normal fake-provider request. Windows exercises safe refusal, never platform spoofing.
Operation journal records remain durable facts and are not evicted to flatten memory.

One child allocation/lifecycle set, normal CI only: same fixed workload, inspector sampling interval4096,
heap before/after quiescence and at like-for-like released/GC checkpoints (after warmup and after workload),
WeakRefs for Agent/queue/observer; two end-of-phase GC turns, never per event.
Output source/fixture/environment stamps, counts and sampled bytes; no timing comparison or savings inference.
A60-second subprocess timeout bounds failure; any assertion/retention failure stops acceptance.
Local development runs only new functional assertions; applicable check/build once; exact Linux/Windows CI
supplies full required tests and the fresh profile. One automatic exact-head review; no duplicate request.

Frame submissions/writes are render-work proxies, not renderer item counts. Artifact/ledger bounds are checked
in their SDK connection; full retained-render counters remain covered by existing tests, not this frame bridge.
No heap-slope claim from a short workload. Sampled allocations are not total or retained heap.
No runtime improvement is claimed; fresh evidence supplements, rather than replaces, accepted phase evidence.

## Compatibility and stop

No runtime edits are planned. A reproduced selected B0/B1 requires a recorded causal freeze exception before a fix.
6A1/6B1 remain frozen: default-off, eligible native Linux only, Windows refusal, private durable receipts,
custom host-history notices, no automatic failed/unknown replay, sidecar-preserving rollback.
No7B,6A2,new adapter,provider wire/storage change,release or merge.
Final SHA, CI, review, observed metrics and findings belong in PR metadata, avoiding documentation commit loops.

Fixture correction during development: replacing streamFunction bypasses the SDK manifest recorder.
The new connection therefore compares actual provider-context prompt/tools, while existing prefix-production
fixtures remain authoritative for manifest hashes. The initial undefined-manifest assertion failed and was
replaced with a nonempty-context assertion, not an undefined-equals-undefined pass. No production defect.

## Batched candidate review correction

Review5145532458 identified six evidence-quality B1s and one README-link C at c725fbb.
No production freeze exception is needed. Observer errors are asserted zero outside callbacks;
both prepared tools must execute exactly once and observe abort before the next run.
An explicit sink gate spans three submissions and requires pending-frame replacement.
The changed read must appear in the actual second provider context, not just canonical history.
Sampling now includes minor/major-GC-collected objects and reports leading sites and bytes/submission/update.
The package README links to repository documentation because docs/ is not in the published file list.
Prior aggregate allocation figures are superseded; replacement CI runs the same one bounded set because
the fixture and sampling were materially corrected, not to seek nicer numbers.7A's original commit remains.
